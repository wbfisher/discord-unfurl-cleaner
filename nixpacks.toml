[phases.setup]
nixPkgs = ["nodejs_20", "sqlite"]

[phases.install]
cmds = [
  "npm ci",
  "npx playwright install --with-deps chromium"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.js"
